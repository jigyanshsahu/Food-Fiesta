// ─── Shared constants across the frontend ───────────────────────────────────

/** Delivery fee in INR — must match the Stripe charge in ordercontroller.js (5000 paise = ₹50) */
export const DELIVERY_FEE = 50;
